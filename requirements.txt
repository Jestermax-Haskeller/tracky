# PySide6 is the official Qt 6 binding. Version 6.11.2 supports Python 3.10-3.14.
PySide6>=6.11.2,<7

# Process information such as executable name and path.
psutil>=6.0,<8

# Windows UI Automation used to inspect supported browser address bars.
pywinauto>=0.6.9
pywin32>=308
comtypes>=1.4.8

# Build dependency used internally by build_installer.bat before Inno Setup packages Tracky.
PyInstaller>=6.22.2,<7
